# 面向智能体的内存管理系统（高校赛题 14）

基于 llama.cpp 扩展的 Agent 长生命周期推理内存优化项目：KV Cache 生命周期管理、分支共享（COW）、Prompt 压缩，配套可复现的 Agent 工作流 Benchmark。

## Project

- 目标：在保证推理效果前提下降低智能体推理的显存/内存占用与延迟（赛题要求优化前后同硬件对比）。
- **具体目标应参见文件`实习题目.md`。**
- 技术栈：llama.cpp（C++ 推理框架，qwen35 架构）+ Python 3.13 / uv（benchmark）+ OpenAI 兼容 API。
- 入口：`benchmark/agent_bench.py`（兼容入口，委托 `runner.cli_main`）；核心优化代码位于 `llama.cpp/tools/server/`（slot 生命周期策略层）与 `llama.cpp/src/`（KV 统计接口）。
- 模型：Qwen3.5-4B（GPU 正式；GGUF `qwen3-5-4B-Q4_K_M.gguf`，当前已下载）/ Qwen3.5-0.8B（CPU 开发；`Qwen3.5-0.8B-Q4_K_M.gguf`，可脚本拉取）/ Qwen2.5-0.5B（最小验证；`qwen2.5-0.5b-instruct-q4_k_m.gguf`，可脚本拉取），GGUF 在 `models/`（不入库）。

## Commands

```bash
# 编译 llama.cpp（在 llama.cpp/ 内）
cmake -B build -DGGML_CUDA=OFF && cmake --build build -j $(nproc)        # CPU
cmake -B build-cuda -DGGML_CUDA=ON -DCMAKE_CUDA_ARCHITECTURES=89 && cmake --build build-cuda -j $(nproc)  # GPU

# 下载模型（models/ 内；模型文件不入 git，必须用脚本拉取）
./download_models.sh          # 全部（0.5b/0.8b/4b）
./download_models.sh 4b       # 单个

# 启动 llama-server（GPU 正式对比；优化机制开关）
./llama.cpp/build-cuda/bin/llama-server -m models/qwen3-5-4B-Q4_K_M.gguf \
  --host 127.0.0.1 --port 8080 -ngl 99 --ctx-size 8192 \
  --kv-unified --parallel 4 \
  --unified-idle-slot-policy default    # 或 lru（experimental：unified 价值感知淘汰）
  --lifecycle-trace                     # 可选：归属诊断（默认关闭，debug-only）
  # --kv-hotness recency               # 可选：session 热度 victim（默认 off）
  # --kv-tiering ram --cache-ram 8192  # 可选：L0→L1 idle/pressure offload（需 --kv-unified）
  # --kv-tiering ram,disk --slot-save-path /tmp/slots --kv-tier2-max-mib 1024  # 可选：L1 溢出到磁盘

# 运行 benchmark（benchmark/ 内，uv 管理依赖）
uv sync
uv run python agent_bench.py --scenario all            # 三场景：multi_turn/tool_call/branch
uv run python agent_bench.py --scenario long_life --long-rounds 40   # 长生命周期（需 --ctx-size 与 server 一致）
```

## Architecture

- `benchmark/agent_bench.py`：兼容入口（E0 重构后为薄壳，委托 `runner.cli_main`；CLI 参数与输出格式向后兼容）。
- `benchmark/framework/`：核心框架——`config.py`（JSON/YAML/dict 配置系统）、`driver.py`（OpenAI 兼容 API 封装，400 兜底 + timings 提取；E15.3 起支持 `preprocessor=` 发送前结构化压缩；M0 起 `chat()` keyword-only 扩展 temperature/seed/max_tokens/extra_body + `sdk_max_retries` 可选 + `/apply-template`、`/tokenize` 精确 token 计数）、`sampler.py`（RSS/GPU 采样）、`workload.py`（Workload 抽象：`generate/run/evaluate` + 注册表）、`prompt_preprocessor.py`（E15.3 B1：deterministic structured-lossless preprocessor，消息级完全重复去重 + protected 保留 + round-trip restore + fail-fast）、`tool_payload.py`（E15.2 ToolPayloadStore）、`context_policy.py`（E15.4 C 线核心）。
- `benchmark/workload/`：分层场景实现——L0（multi_turn / tool_call / branch）、L1（long_life / branch_pressure / M0 fanout 相关）、L2（realistic_agent：规划、工具链、失败重试、状态更新、校验与总结），导入即注册；结果结构与旧脚本一致（行内追加 `timings` 键）。
- `benchmark/metrics/`：p50/p95/mean/std/cache_hit_rate/summarize（保留旧字段）。
- `benchmark/runner/`：场景 × repeat × warmup 编排、结果落盘 `results/bench_<ts>.json`（`{config, summary, scenarios}`）；`e15_branch_concurrent.py`（E15.1 分支并发 paired）、`e15_3_b1_paired.py`（E15.3 B1 4B greedy paired 门禁）、`fanout_prompts.py`（M0 决策/分支 prompt 纯函数 + MATRIX_PLAN 单一来源）、`m0_schema.py`（M0 canonical 22 键 schema/builders/validator/atomic writer + 退出码 64/65/70/74）、`m0_fanout_runner.py`（M0 正式 CLI：15 server 生命周期 + 24-unit 矩阵 + gates + 落盘）、`m0_calibration_runner.py`（v56 入库校准复现入口：`cd benchmark && uv run python -m runner.m0_calibration_runner --server-bin ../llama.cpp/build-cuda/bin/llama-server --model ../models/qwen3-5-4B-Q4_K_M.gguf --out ../benchmark/results/m0_cal_4b_runN.json --tmp-dir ../benchmark/results/m0_cal_4b_runN_tmp --run-id m0-cal-runN`；参数化无硬编码路径，复用 M0FanoutRunner 生命周期，执行 calibration + off/on decision-validation + 可选 parallel10 探针，报告自带 server_logs_summary 结构化摘录，cleanup 保证）。
- `benchmark/report/`：markdown 实验报告。
- `benchmark/configs/`：示例配置（example.json / example.yaml）+ q8_0 配置（`qwen35_4b_q8_validated.yaml` 为 E13.1 固化；`qwen35_4b_q8_production.yaml` 为 E14.1 固化）。
- `benchmark/tests/`：pytest（不依赖 GPU/真实 server，含 mock OpenAI server e2e 冒烟）。
- `benchmark/baseline/`：正式基线归档（可读命名 `<模型>_<环境>_<场景>_<说明>.json`）；`benchmark/results/` 为运行期临时输出（不入库）。
- `llama.cpp/`：上游框架（独立 git 仓库，根仓库不跟踪）；KV 优化改动在其中实施并内部提交。
- `models/download_models.sh`：可复现模型拉取。

## Conventions

- **可自动运行 llama-server / benchmark**：宿主机可访问 GPU（RTX 4060 8GB，驱动 610.43.03）；只要用户没有要求不能自动运行，都可直接运行。注意耗时（all 场景约 5–10 分钟、long_life 40 轮约 10–20 分钟）与显存（4B 模型约占 3.2GB，8GB 卡需留意并行 slots 与 ctx 大小）。
- **pkill/pgrep -f 会匹配 bash 作业自身命令行**：`bash -c` 把整条命令文本（含启动 llama-server 的命令）放入进程 cmdline，`pkill -f "llama-server ..."` 或 `pgrep -af "build-cuda/bin/llama-server"` 会匹配到执行该命令的 bash 作业自身，导致"自杀"（把自己杀掉，新 server 也随之未启动；2026-08-06 bash-4 事故）。避免方法：① 杀进程用 `pkill -x llama-server`（精确进程名，不匹配命令行文本）；② 查询进程用 `pgrep -x llama-server`；③ "先杀 → 确认无残留 → 再单独启动"必须拆成独立命令/作业，绝不在同一 bash 命令行里既 pkill 又启动同一模式的服务。
- **GPU 显存采样问题**：容器内 pynvml 报 `NVMLError_DriverNotLoaded`，`peak_gpu_mb` 返回 null 说明 GPU 未加载，需要暂停工作等待用户处理。
- **实验结果归档**：正式结果移入 `benchmark/baseline/` 并改可读文件名；`results/` 只留临时输出（.gitignore 已忽略）。
- **git**：`llama.cpp/` 独立仓库不纳入根仓库；`models/*.gguf`、`build*/`、`.venv/` 不入库；用户要求**每个阶段完成后都提交代码**（授权提交后执行提交，并同步更新相关文档中的 git 提交状态说明；未经授权不提交）。
- **commit message 格式**：标题 `<feat|fix|chore|docs|refactor，可多个用 & 连接如 feat&fix>: <摘要>`，空一行后分点（`- ` 开头）详细描述改动内容。
- **模型能力限制**：Qwen3.5-0.8B 指令遵循不稳定（工具参数可能填错）；默认 `--no-think`/`enable_thinking:false` 防思考循环。
- **不要自己尝试安装软件包**：如果需要安装非普通 uv Python 依赖的软件包，需要使用 root 权限安装软件，或者需要写入不可写目录，不要自己操作，请停下来让用户操作。
- **AGENTS.md 必须及时更新**：当命令、目录结构、约定或架构发生变化时，本文件应在该变更落地后立即同步更新，保持准确——这是每个 agent 与协作者的职责，不要等到项目结束时才补。
- **用户要求执行已经达成的目标时要告知用户已实现**：用户要求执行某操作（如清理文档中冗余内容、清理冗余代码、确保符合xxx约束等等），如果用户要求达到的目标已经达到而无需操作，应该告知用户。

## Notes

- E0（Benchmark 基础设施重构）已完成：`framework/ workload/ metrics/ runner/ report/ configs/` + 42 个 pytest（E0 阶段实测，见 `docs/E0_IMPLEMENTATION_REPORT.md`）；**当前全仓累计 pytest = 758**（2026-08-09 v68 实测：`cd benchmark && uv run pytest -q` → `758 passed`；758 = 752 + 6（v66 21→25：文本 fallback 收紧/误匹配反例/共享 helper 一致性 4 例；v67 3→5：dv 崩溃边界参数化第 1/5/10 请求 3 例 + 第二 session 崩溃 + 无崩溃反 e2e）；752 = 749 + 3（v67：dv 首 session 崩溃 e2e + 第二 session 崩溃 e2e + 无崩溃反 e2e）；749 = 728 + 21（v66：wrapper 单元 5 + 分类单元 8 + 阶段覆盖 5 + rep peak 采样 3）；728 = 725 + 3（v65 新增 rep 延迟字段测试 build_rep 写入/可选/validator 接受）；725 = 722 + 3（v63 新增 G-M0-5 memory 分支 + RS 多行取首个 2、v64 新增 stop 正常 detail 不记 warning 1）；722 = 716 + 6（v62 新增 `test_m0_v62_review.py`：stop kill 兜底矩阵继续 1、kill 仍失败 FormalIncomplete 1、detail 落 notes 1、start 失败不重复 stop 1、no-process clean 1、纯文件名 out tmp 父目录 cwd 1）；716 = 706 + 10（v61 新增 `test_m0_v61_review.py`：run_formal finally stop 调用计数 3 + stop 契约收紧 3 + main tmp 父目录 2 + 真实短进程端口释放 1）；706 = 690 + 16（v60 新增 `test_m0_v60_review.py`：_stop_server 返回 (ok,detail) 4、formal _connect/_capture_baseline 基础设施归因 4、cleanup_stale_dirs marker 并发安全 7、_cleanup_all 删除 1）；690 = 673 + 17（v59 新增 `test_m0_v59_review.py`：calibration 日志路径 r.tmp_dir 3、formal dv partial 落盘 3、_stop_server 异常 4、日志 pattern 常量 2、cleanup_stale_dirs 5（含递归最新 mtime 活跃判定 3））；673 = 659 + 14（v58 新增 `test_m0_v58_review.py`：run_decision_validation 同步/清空/中途异常保留 3、calibration decision_validation 标准结构 1、CalibrationLengthMissing 3、日志流式/fallback/started_at RFC3339 UTC 4、--tmp-dir 父目录安全/唯一子目录/keep-tmp 3）；659 = 639 + 20（v57 审查修复新增：`test_m0_calibration_runner.py` 11→28（+17：v55 旧格式向后兼容 2、meta 映射/日志刷新 2、tmp 清理 4、cleanup 采样 PID 4、异常保留 2、口径/阶段断言 3）、`test_m0_runner_fixes.py` 49→52（+3：warmup ServerError 死/健康/内部 bug）；`test_m0_schema.py` 89 例与 `test_m0_v54_validation_controls.py` 数据适配 v57 语义）；639 = 628 + 11（v56 新增 `test_m0_calibration_runner.py`：CLI 参数 3 + mock e2e smoke 3 + representative_output/sha256 合同 5；628 为 v55 复审实测）；619 = 613 + 6（v52 新增：`test_m0_v51_review.py` 校准真实 openai SDK 异常归因 2（InternalServerError/APIConnectionError，替换 patch run_calibration 抛 HTTPError 旧法）+ warmup 决策异常正反 2 + 校准后 clean_all_slots (0,0)/异常/死 server 3，净 +6）；613 为 v51 复审实测（commit 32ab209）；574 = 558 + 16（v48 复审新增：G-M0-4 真实 build_group 产物三态 + derived 不参与 gate + parse_group_id 匹配、server 命令合同 slot-save-path、决策 ERROR rep erase/after_erase 正反、periodic 高峰/异常路径、G-M0-2 缺分支观测、warmup 异常后 poll 正反、sampler 端口兜底 3、atomic tmp 唯一名/活跃不删）；558 = 537 + 21（M0 Critical 修复新增 `test_m0_runner_fixes.py`）；537 = 413 + 124，其中 413 为 M0 前全仓累计（= 177 + 236；177 = E1–E4 阶段实测已含 E0 的 42；236 = E15 新增：E15.1 branch_concurrent 48、E15.2 tool_payload 65 + 集成 21、E15.3 preprocessor 34、E15.4 context_policy 68），M0 **新增** 124 个：fanout_prompts 22、m0_schema 80、m0_fanout_runner 22（含 mock e2e/CLI）+ 21 Critical（test_m0_runner_fixes）；均在 benchmark/ 内，不依赖 GPU/server；注意必须在 `benchmark/` 目录下运行，否则 pytest 会收集到 `llama.cpp/` 子仓库的测试）。
- E1（llama.cpp 可观测性）已完成：`GET /metrics/kv`（KV 统计）+ KVProbe；详见 `docs/E1_*`。
- E2（生命周期候选探索）已完成：A2 prefix-branch 路由实现后 REJECT 冻结；`--cache-ram` 默认 8192；详见 `docs/E2_*`。
- E3（A1/A4 统一 idle-sequence 价值感知回收）已完成：`--unified-idle-slot-policy default|lru`（lru experimental，默认 default）+ `--lifecycle-stats` + `--lifecycle-trace`（归属诊断）；真实场景收益稀释 → KEEP_EXPERIMENTAL；性能门禁因 Laptop GPU 抖动 HOLD；详见 `docs/E3_*`。
- E4（内存容量验证）已完成：并发承载 ≥8 session、OOM 边界 >96%（exploratory）；详见 `docs/E4_*`。
- E3.6（真实流量回放）**未执行**：硬前置 = 用户提供且 validator（`benchmark/schemas/lifecycle_trace_v1.json`）通过的真实匿名 trace。
- E6-E8（KV 优化实施与复核）已完成：C1 attention-only KV 优化真实且正确（TinyLlama 收益），但主模型 Qwen3.5-4B（hybrid）无收益 → E7 起 `PARTIAL_KV_CORRECTNESS_NO_OPTIMIZATION`；tenant 限定 TRUSTED_SINGLE_TENANT；详见 `docs/E6_* / E7_* / E8_*`。
- E9-E13（C1 关闭 + q8_0 生产化 + 主模型转向）已完成：C1_ATTENTION_ONLY_PASS；q8_0（`--cache-type-k/v q8_0`）部署 profile 固化 `PASS_VALIDATED_DEPLOYMENT_PROFILE`；checkpoint 主模型路径 E12 NO_GO_WITH_EVIDENCE（prototype 保留 experimental attention-only）；详见 `docs/E9_* / E10_* / E11_* / E12_* / E13_*`。
- E14（发布/灰度/运维交接）已完成：`RELEASE_STATUS: READY_FOR_DEPLOYMENT`（q8_0 production profile，15 项验收 + 回滚演练通过）；**真实生产灰度未执行**（`PRODUCTION_ROLLOUT_STATUS: NOT_EXECUTED`，无生产环境，不虚构）；运维交接见 `docs/E14_5_OPERATIONS_HANDOFF.md`；详见 `docs/E14_*`。
- E15（E14 后独立扩展，四技术线分阶段，方案见 `docs/E15_0_TECHNICAL_PLAN_AND_ACCEPTANCE.md`）：**E15.0** 方案已提交（根 `5dd6b79`）；**E15.1** 分支并发共享不变量加固 + 真实 fan-out 验证 **PASS**（根 `f357a42` / llama.cpp `4a699aaad`，零核心代码改动；未跑 4B——hybrid 由 capability gate 永久禁用共享）；**E15.2** ToolPayloadStore 外置存储 + workload 集成已提交（根 `a2e0558`；**未跑真实 4B paired**，收益待 §6 后续动作）；**E15.3** B1 deterministic structured-lossless preprocessor（消息级重复去重 + protected 保留 + restore/fail-fast，配置 `config.extra["preprocessor"]` 默认 off）已提交；**4B greedy paired 无损门禁不成立**（single 压缩生效但输出不一致 → `HOLD_NOT_VALIDATED` 降级有损转 E15.5；multi_turn 短中文块 token 无收益 → `HOLD_NO_MEASURABLE_GAIN`；restore 对照确认实验有效；llama.cpp 未改）；**E15.4** C 线上下文策略独立核心 **DRAFT** 已提交（根 `c9c26ab`；未集成 workload、未跑真实模型 paired）；**E15.5（有损摘要 oracle）/ E15.6（完整验证收口，含 4B 门禁）尚未完成**；详见 `docs/E15_*`。
- **M0（阶段 0 后首条路线）**：**状态：已收口（2026-08-09，根 `3ae6bb6`；两层结论 A benchmark/归因门禁 PASS / B on 机制优化收益 NOT_APPLICABLE/NO_GAIN——4B hybrid 共享被 capability gate 拒绝，见 `docs/M0_FINAL_RESULT.md`）**；真实智能体多路径决策 workload + 分支内存基线——**调研完成 + 设计完成（v47 终审一致性收口——无 Critical/High；EXPECTED_UNIT_IDS 单一定义/对照 A=off B=on 显式/崩溃边界正反例/测试㉗ 集合段合并；modes 只含已启动 groups + 失败 group status=ERROR；崩溃第 5 个 rep → matrix_complete=false 与请求级 REP_ERROR 严格区分；MATRIX_PLAN 合法组合映射（fanout2→3 桶/fanout4→2 桶/fanout8→1 桶 × 2 profile × 2 control = EXPECTED_UNIT_IDS 精确 24）；集合不变量（observed ⊆ EXPECTED_UNIT_IDS−rejections、executed_units==len(complete_unit_ids)、complete ⊆ observed、matrix_complete=true 时 observed==complete==EXPECTED−rejections、false 时允许任意子集、不限于前缀；partial rep 计 observed 不计 executed）；EXPECTED_UNIT_IDS 常量 = MATRIX_PLAN 重建 24 unit_id；§3.5/§3.7 堆叠 vNN 标注清理）；MATRIX_PLAN 常量 FORMAL_REPS=5/WARMUP_REPS=2、CLI 删 --reps/--warmup（显式传 → EXIT_USAGE 64）；replicates 为逐 rep 对象（unit_id/rep_index/fanout/bucket/ctk/ctv/prefix_len:int/branch_len:int/server_group_id/status/decision_fallback/error_type?/error_stage?/error_bucket? + §5.2 metrics），不存在独立 unit 对象；formal group 失败 phase 规则唯一权威（首 group 启动失败 → preflight；第 2+ 启动失败/运行崩溃 → FORMAL_INCOMPLETE，删除 v22 无条件段）；测试编号 ①–㉗ 顺序排列（§6 权威范围同步）；测试㉖ formal server 启动失败/验证证据、测试㉗ 多 cache profile 编排 + 首/中途 group phase + FORMAL_INCOMPLETE 集合断言；--fanout/--prefix-len/--branch-len/--ctk/--ctv/--parallel/--reps/--warmup 均未知参数 EXIT_USAGE 64），；**第 2 步实现完成（2026-08-09）：fanout_prompts.py / m0_schema.py / m0_fanout_runner.py + driver 扩展 + mock_server /apply-template,/tokenize,crash 支持 + 124 新测试全绿（537 passed）+ mock CLI smoke 生成合法 schema 结果；未跑正式 4B 矩阵、llama.cpp 零改动**；**f8334bd 复审 11 项生产路径门禁修复（2026-08-09，commit 后附）：G-M0-4 经 sch.parse_group_id(server_group_id) 匹配（build_group 不写 ctk/ctv/fanout 顶层字段，禁止 grp.get 读取；测试用真实 build_group/build_modes 产物）；derived formula 仅 internal consistency detail、不参与 gate status（PASS 须真实 RS 日志独立观测且容差内、缺观测 NOT_APPLICABLE、超差 FAIL；RS_BYTES_PER_ROW=24×548864×4 为 0.8B 实测系数、4B 正式矩阵必须重校准不宣称已验证）；_server_cmd 加 --slot-save-path <tmp_dir>（llama.cpp server-context.cpp:5448 前置，否则 POST /slots erase 返回 NOT_SUPPORTED；与 e15 runner 一致）；决策请求 ERROR rep 不再提前 return——统一 erase+after_erase 路径（server 健康 → metrics.kv 真实样本归零、G-M0-3a 验证；崩溃 → ServerCrash/FORMAL_INCOMPLETE 不伪造观测；end_run 移到 erase+after_erase 之后——否则 after_erase 样本 run_id=None 不进 rep 聚合的根因修复）；每 rep begin_run 后 start_periodic/stop（覆盖分支并发与工具轮，peak_used_cells/peak_active_sequences 来自真实周期样本，异常路径 finally stop）；warmup 任意请求异常后立即 poll（已退出 → 不忽略转 group ERROR/FORMAL_INCOMPLETE，仍健康才允许忽略）；sampler 端口兜底只接受显式 `--port <port>`/`--port=<port>` 进程（正则避免 8080 匹配 80809，否则 None）；atomic tmp 改 `.m0_fanout_<name>.<pid>.<rand>.tmp` 并发安全唯一名 + 同目录 os.replace；cleanup_stale_tmp 仅删超过 age 阈值（main 传 --cleanup-tmp-age，默认 3600）的陈旧文件、活跃并发写不删；**；**a408031 审查 10 项 Critical 修复（2026-08-09，commit 后附）：KVProbe 数据路径（baseline 从 snapshot['data'] 读 capacity、per-rep begin/end_run 边界、erase 后采样 after_erase、G-M0-3a/G-M0-5 缺观测/非零 → FAIL 不静默 PASS、G-M0-2 缺分支观测 FAIL）；Driver.chat 顶层 finish_reason（choice.finish_reason，runner 不再从 timings 判定，mock 可配置 length → invalid_length>0）；_connect 传实际 host/port + sampler 兜底按端口 net_connections 过滤；启动 OSError（bin 缺失/不可写日志）→ ServerError → preflight 落盘 rc==0 无 traceback（run_safe 兜底 EXIT_SOFTWARE 70）；CLI 入口 cleanup_stale_tmp；prefix_len/branch_len 用 (bucket,fanout) 校准实测值（apply-template+tokenize，validator 同桶一致性不变式）；warmup 崩溃立即 group ERROR/FORMAL_INCOMPLETE；mock_server 删实例 count 死属性；G-M0-5 真实日志串 `E8-C1: capability rejected: hybrid`（server-context.cpp:1505，--kv-prefix-share+hybrid 时输出）；G-M0-4 拆 derived 自检 + RS buffer 独立观测（llama-memory-recurrent.cpp:115 `RS buffer size = X MiB`，无观测 → NOT_APPLICABLE）；558 passed（v48 后 574）**；**未跑正式 4B 矩阵（llama.cpp 零改动）**；详见 `docs/M0_BRANCH_MEMORY_BASELINE_DESIGN.md`（实施建议 GO）。关键探针结论（4B Q4_K_M + ctx4096 + unified，RTX 4060 8GB）：**32 层 = 8 attention + 24 recurrent**（qwen35.cpp fallback interval=4）；attention KV 固定池 68 MiB(q8_0)/128 MiB(f16) 与分支数无关（per-cell q8 17408 B = 8×8×128×2×34/32）；recurrent state **50.25 MiB/slot** 随 parallel 线性增长（= 24 层 × (n_embd_r 24576 + n_embd_s 524288) × 4B，实测闭合）；parallel 10 q8_0 峰值 3402/8188 MiB 无显存阻塞；合法矩阵 = fanout{2,4,8}→parallel{4,6,10} × 桶 short(150/150)/medium(280/480)/long(400/600)，预算函数 `budget=P+N×(P+B) ≤ 3481 cells` 逐 unit（**P/B 由 /apply-template + /tokenize 实测**；一致性门禁 = chat `usage.prompt_tokens` vs tokenize 偏差 0，**证据归档 `benchmark/baseline/m0_probe_thinking_token_parity_20260809.json`**，add_special 幂等归因 = 该 GGUF `add_bos_token=false`、bos_token_id 缺失、add_eos_token 字段缺失（无 BOS），换模型必须重验）；`/metrics/kv` 只统计 attention（recurrent 无运行期接口；回收门禁仅 attention，GPU 回落仅 smoke）；架构定稿独立 runner（不注册第五场景）；driver 扩展保留 `_retry` 位置参数（driver.py:132 递归兼容、_extra_body :135-142）+ `sdk_max_retries` 可选参数（M0 实例化 `Driver(max_retry=1, sdk_max_retries=0)`，6-wire 上限；context400 递归透传参数）；thinking 合同：server 默认 thinking 开启，必须显式 enable_thinking=false（server-common.cpp:1095-1102 + 证据）；**决策谓词 → rep `status=INVALID_DECISION`（⇔ decision_fallback=true）；rep 状态机 OK/INVALID_DECISION/ERROR；G-M0-1 专用验证 <100% → G-M0-1=FAIL 但 formal 仍请求模型；5b 三触发（启动失败/崩溃且 session 不完整/序列化失败）、验证崩溃/启动失败边界（v23-v30）、验证请求 retry 三路；formal server 启动失败保留完整证据、三类 code；**SCHEMA_INVALID 落盘合同（v43）：**唯一权威 canonical meta 表（**22 键（v43：prefix_len/branch_len 移 per-unit 精确字段、prefix_bucket/branch_bucket 合并单 bucket、ctk/ctv 合并 cache_profiles）；model_id 唯一；nullable 白名单 5 键（parity_compensation 必填但允许 null 恒 null）；decision_validated 仅 decision_validation 完整且 G-M0-1=PASS 时 true、decision_fallback 仅 formal 任一 rep fallback 时 true；**parity_ok/token_count_method 总规则（v35，不按 preflight_reason 硬编码）**：parity 全部桶完成且偏差=0 → true、全部完成但偏差≠0 → false、未完整完成 → null；token_count_method 完整执行 apply-template+tokenize 时 = 该字符串（无论 pass/mismatch）、未完整建立 → null；validation_incomplete、验证/formal server_crash/health_failed/endpoint_unavailable 若 parity 已通过均 true+apply-template+tokenize（validation_incomplete 差异字段含 parity_ok）；parity_progress 保留实际进度（validation_incomplete 不恒空）；SCHEMA_INVALID 专有值清单仅 canonical 表一处权威定义、全文不复制不内联不完整字段）**；**不可 null 默认值**（binary_version 未知 → unknown、整数运行配置来自 CLI/矩阵合同不得魔法 0、字符串不可空串）；**tmp kind 统一 {main, sidecar}**；**decision_validation 两者均 null**；**fanout_prompts.py 只构造 doc、写入委托 m0_schema.py atomic writer**；**source_phase：普通 PREFLIGHT_INFRA 即使 formal server 启动触发也 = phase=preflight，仅 schema_invalid 记录原触发 formal**；**SCHEMA_INVALID 为 parity 总规则固定错误报告例外（v35：parity_ok/token_count_method 恒 null、parity_progress 恒空，不信任原始对象）**；**CLI/config 与预算职责单一化（v35：CLI 仅校验语法/枚举/类型/结构非法 → EXIT_USAGE=64 不落盘；budget 超限统一 budget 函数 → preflight_rejections/ALL_BUDGET_REJECTED 落盘不由 CLI 拦截）**；**parity_progress.completed 权威全集 short-P,short-B,medium-P,medium-B,long-P,long-B（v35）**；**exit constants 统一 64/65/70/74（m0_schema.py）**；测试㉔ budget-only / ㉕ CLI-only / ⑯ canonical 表派生键集；**planned_units=24 语义（v36：静态预算预筛 6 组合×2ctk×2control；long×4 等预筛排除不写 preflight_rejections；rejections 仅记精确复核后进一步超限的 24 候选）**；**parity 交叉不变量（v36：parity_ok∈{true,false} → completed 严格等于权威有序 6 项；parity_ok=null → 真前缀；SCHEMA_INVALID 例外 null+空）**；**CANONICAL_META_KEYS 常量单一来源 + build_preflight_envelope 签名合同（v37 名称统一）**；CLI 不暴露 --parallel；**PARITY_MISMATCH 全桶统一判定（v37：完成 6 桶后判定、偏差不 fail-fast、parity_ok=false+completed 全集+errors 记 parity_mismatch；仅基础设施中断才 null+前缀）**；**completed 语义（v37：流程完成即计入即使 mismatch；基础设施失败桶不计只入 errors）**；**ALL_BUDGET_REJECTED 仅 24 候选精确复核全部被拒（v37/v38：preflight_rejections 永不含静态 3 组合）；parallel:=fanout+2 恒等派生**；**全桶 parity 执行语义（v38：数值 mismatch 不单桶 fail-fast、完成全部 6 项后统一判定；仅基础设施中断 → null+前缀；混合场景 mismatch 条目保留证据不丢）**；**parity_progress.errors 增 template∈{P,B}（v38），唯一性键 (code,stage??null,bucket??null,template??null)**；**测试⑯ CANONICAL_META_KEYS 常量驱动（v38）**；**两个 error schema（v39）：parity_progress.errors 四元组 {code,stage?,bucket?,template?}（parity_mismatch 必须 stage=calibration/bucket/template 完整）；error_summary 三元组 {code,stage?,bucket?,count} 不引入 template**；**混合场景顶层 preflight_reason 只记基础设施 code、parity_mismatch 不进 reason（v39）**；**CLI 删 --ctk/--ctv，top meta 固定 cache_profiles=[q8_0/q8_0, f16/f16]，12 server group 编排（v40：cache profile × fanout × control = 2×3×2，每组 parallel=fanout+2、独立生命周期、崩溃归 group → matrix_complete=false）**；**per-unit 字段（v43：unit_id/fanout/bucket（单 bucket，prefix 与 branch 同桶）/ctk/ctv/prefix_len/branch_len/server_group_id/rep_index；control 由 modes 父级表达；unit_id 允许 5 rep 重复、(unit_id,rep_index) 全局唯一）+ G-M0-4 按 group 归因 + validator 24 unit_id 去重集合唯一完整（扣除 rejections 后 executed 匹配）**（**v43 历史，已被 v45/v46 取代——v47：executed=len(complete_unit_ids)、complete 仅由 rep_index 完整性判定、不用 len(去重 unit_id)==executed 等式；见下文集合不变量与 §5.1/测试㉗）**；**rep 粒度（v43）：replicates 每项=正式 rep（warmup 不落 replicates、group warmup_count=2 必填）、每 unit 固定 5 reps、rep 级 status/decision_fallback/metrics、已执行 unit rep_index 集合 {0..4}**；**FORMAL_INCOMPLETE 子集规则（v43 历史，已被 v45 取代——v45 起 complete 仅由 rep_index 完整判定、执行 completed==len(complete_unit_ids)，不再用 len(去重)==executed_units）：modes 只含已启动 groups（失败 group 以 ERROR 出现、未启动不出现）；matrix_complete=false 时 len(去重 unit_id)==executed_units（仅完整 5 reps 计 executed）；仅首 formal group 启动失败 → preflight，第 2+ 启动失败/运行崩溃 → formal incomplete**；**parity_progress.errors 四元组/error_summary 三元组分 validator（v43）**；**测试⑯ 常量参数化（CANONICAL_META_KEYS + SCHEMA_INVALID_FIXED）、测试㉗ 多 profile 编排（v40）**；**CLI 一次恒跑完整 24-unit 矩阵（v41：删 --fanout/--prefix-len/--branch-len，MATRIX_PLAN 定义维度）**；**top meta 键数 22（v41：parallel/fanout/prefix_len/branch_len 移 per-unit；ctx_size/cache_profiles 仍 top）**；**per-unit 权威表 + ID 格式（v41：unit_id={control}:{ctk}-{ctv}:f{fanout}:{bucket}、server_group_id 前缀；validator 校验 24 集合）**；**15 进程 = 校准1+验证2+formal12 groups（v41）；group 失败统一停止剩余矩阵（FORMAL_INCOMPLETE 优先、首组失败才 executed=0）**；**§6 SCHEMA_INVALID_FIXED/MATRIX_PLAN/EXPECTED_UNIT_IDS 常量（v41）；G-M0-4 按 group 对账**；**全部 unit 预算被拒 → 全部 24 候选精确复核被拒（v39，3 处）；混合场景测试移㉑**；测试⑯ 逐字段覆盖 + phase 错配反例；**validation_incomplete 与 schema_invalid 差异字段**（schema_invalid parity_progress 恒空、schema 错误只在 sidecar；validation_incomplete 属 PREFLIGHT_INFRA、parity_progress 保留实际校准进度）；**source_phase validator**（普通结果必须 ==phase；SCHEMA_INVALID 例外 phase=preflight 且 source_phase∈{preflight,formal}）；**decision_validation 保守恒 null**（不信任子结构、formal 来源也不复制，与 PREFLIGHT_INFRA 保留独立验证证据路径不同）；**atomic writer 共用**（普通主结果也走 m0_schema.py atomic writer，无第二套非原子写入）；测试⑯ 逐字段覆盖；**SCHEMA_INVALID 落盘：专有值清单仅 canonical meta 表一处权威、全文不复制不内联（本处不展开）；段落仅保留特有说明（notes sidecar 路径、禁止递归、自检 70）；phase 合法组合加 SCHEMA_INVALID（测试⑯/映射表覆盖）；写入流程 5 步（validate→构造→自检 70 无文件→临时文件+fsync+先 rename sidecar 再主 envelope+fsync 目录→部分落盘语义）；⑬b-d validation_incomplete 用同一 canonical builder（exit 0）；sidecar 细化（RFC6901、类型白名单、归因 12 码不适用）；退出码 0 含所有合法落盘（65 除外）；⑬b-f 改「保留合法 envelope+结构化 sidecar 证据」；§6 分配 m0_schema.py + test_m0_schema.py；sidecar 独立 validator 错误码枚举（missing_key/extra_key/type_mismatch/enum_mismatch/invalid_value/invariant_violation，path JSON Pointer、类型名不保存值）；validator 拆分 validate_result_envelope/validate_schema_sidecar；退出码 0（合法结果成功落盘含 INVALID 但 65 除外）/64（EXIT_USAGE：CLI/config preflight 解析失败 → stderr INVALID_CONFIGURATION、不落盘；配置完整后构造结果对象 schema 非法才走 65）/65（EXIT_SCHEMA_INVALID 完整证据对）/70（EX_SOFTWARE INTERNAL_SCHEMA_ENVELOPE_BUG 不递归包装）/74（IO_WRITE_FAILED）；写入顺序（内存验证→临时文件+fsync→先 rename sidecar 再主 envelope；无主 envelope 时 sidecar 仅诊断）；⑬b-f 不落原始构造 JSON；source_phase 覆盖 preflight/formal；finally 不删 results 主/sidecar**；verdict 五值枚举 + 稳定规则名称 + 聚合签名；**planned_units 派生值；结构化错误码（12 枚举）；preflight_reason 映射（含 SCHEMA_INVALID→仅 schema_invalid）；token_count_method 彻底唯一；chars/2.0 正式诊断、chars/2.5 仅旧估算对照；ACTION 统一 branch(bX)；测试①–㉗（⑬a-f 六子例、⑰ 清理固定序列 + results 主/sidecar 保留断言、㉖ 5b rejections 正反）**；M0 不用 OpenAI tools schema；探针二进制 8569/afbf375c6（核心代码与 HEAD 等价）。****v54/v55 真实 4B GPU 短校准实测（2026-08-09，未跑 24-unit formal 矩阵）**：构建 build-cuda 8570(4a699aaad) 全链路真实运行；parity 6 桶全对（errors=[]，三次运行一致，calibrated P/B：short/2 260/344、medium/2 420/504、long/2 551/635、short/4 274/359、medium/4 434/519、short/8 302/386）；24 候选预算 0 拒绝；decision-validation off/on 各 10/10 valid、确定性 100%（输出哈希全同）；RS 实测 201.00(p4)/502.50(p10) MiB = 50.25 MiB/parallel 与 RS_BYTES_PER_ROW 常量一致（注释更新）；KV 68 MiB（8 layers q8_0）+ /metrics/kv capacity_bytes=71303168；G-M0-5 日志存在（on 有 off 无）；GPU pynvml 峰值 3358 MiB（需 uv sync 装 nvidia-ml-py + `uv run python` 运行校准脚本）；修复真实首跑暴露 bug（v55）：_server_cmd 端口错配（--port 用已 +1 的 _next_port → health 永远超时，改显式 port 参数 + 回归测试）、-lv 5 日志级别（RS buffer 行默认 verbosity=3 不打印，G-M0-4 观测前置）、v54 decision-validation 双 control（VALIDATION_CONTROLS=off/on，session 记 control 字段，test_m0_v54_validation_controls.py 5 例）；证据归档 benchmark/baseline/qwen35-4b_gpu_m0_calibration_20260809.json + qwen35-4b_gpu_m0_probe_evidence_20260809.json；具备进入完整 24-unit formal 矩阵条件（parity/决策门禁前置全过）。****v56 校准可复现性收口（2026-08-09，commit 后附）：**新增入库入口 `benchmark/runner/m0_calibration_runner.py`（`cd benchmark && uv run python -m runner.m0_calibration_runner --server-bin ../llama.cpp/build-cuda/bin/llama-server --model ../models/qwen3-5-4B-Q4_K_M.gguf --out ../benchmark/results/m0_cal_4b_runN.json --tmp-dir ../benchmark/results/m0_cal_4b_runN_tmp --run-id m0-cal-runN`），参数化无硬编码工作树绝对路径、复用 M0FanoutRunner 生命周期（calibration + off/on decision-validation + 可选 parallel10 探针）、finally 保证 cleanup；**历史临时脚本 results/m0_cal_4b.py 由入库入口取代、不作为复现入口**；CLI/smoke 测试 test_m0_calibration_runner.py 11 例（mock 不依赖 GPU）；**decision-validation 每 session 增 validator 认可的代表性输出合同（v56）**：`representative_output`（第一条 valid 请求原始文本，必须通过 parse_action）+ `representative_output_sha256`（64-hex，可重算，validator 校验 sha256(text)==声明值）；**三份真实 4B GPU 短校准证据（v56，run_id 区分）**：run1（v55 格式 qwen35-4b_gpu_m0_calibration_20260809.json）+ run2/run3（v56 格式 qwen35-4b_gpu_m0_calibration_run2/run3_20260809.json）+ 汇总 qwen35-4b_gpu_m0_calibration_summary_20260809.json——**三份核心字段完全一致（parity 6 桶全对 errors=[]、calibrated_lengths 逐桶一致、preflight_rejections=[]、dv off/on 各 10/10 valid 确定性 100%、RS 201.00/502.50 MiB、GPU 峰值 3358/probe 3354 MiB）**，representative_output 均为 `ACTION: branch(b1)`、sha256 `03c79f28…` run2/run3 完全一致且可重算；**server 日志结构化摘录随 baseline 归档（v56）**：`server_logs_summary`（每 server tag/run_id/pid/phase/started_at + key_lines：RS buffer/capability rejected/KV alloc），pids_seen（4 PID）与 summary pid 一致，不依赖 gitignore results 日志；probe 证据明确 gpu_rss_samples（周期采样多 PID）与 probe_p10（静态单 PID 不同阶段）区分；**正式矩阵日志策略 v56 定稿**：完整 -lv5 日志留 tmp/results（gitignore 不入库），canonical/baseline 只归档 key lines；**639 pytest 全绿（v56 实测 `cd benchmark && uv run pytest -q`）**；仍未跑 24-unit formal 矩阵、llama.cpp 零改动。；**v57 审查修复（2026-08-09，Medium 7 项 + Low 收口，commit 后附）：**① decision session validator 向后兼容 v55（两代表字段同时缺失按旧格式接受；任一存在则两者必须同时存在且 hash 可重算；字段存在时 valid>0 必须代表输出非空、valid=0 可同空；补旧 run1 校验与 valid>0 空反例，`test_m0_schema.py` 89 例数据适配）；② `_start_server` 成功时记录 tag→{port,pid,started_at,log_path} 真实映射，`server_logs_summary` 只用该映射（删除 sorted index/端口偏移推断；启动失败/缺 tag 不错配后续 PID；stop 后重读盘刷新 key lines）；③ calibration tmp 仅报告成功写盘后且非 --keep-tmp 才删除（runner 异常/写报告失败默认保留排障）；④ cleanup.leftover_pids 只检查本 runner 采样 PID 集合（peak.pids_seen + `_pid_alive`），删全系统 pgrep（共享其他 llama-server 不算残留，method 字段声明）；⑤ calibration 异常时报告保留 parity_progress/calibrated_lengths/preflight_rejections 已完成部分与结构化 error（{code,stage,count,detail}），dv 阶段保留 partial sessions；⑥ server_logs_summary started_at 用真实启动时刻、probe_p10 显式 stage 键、gpu_rss_samples.comparability 明确 peak_rss 跨 v55/v56 采样口径不可比；⑦ warmup 基础设施 catch 纳入 ServerError（ServerError 前移定义，健康忽略/死转崩溃），AssertionError/KeyError/TypeError 仍不被捕获 → EXIT_SOFTWARE 70；**659 pytest 全绿（v57 实测 `cd benchmark && uv run pytest -q`）**；仍未跑 24-unit formal 矩阵、llama.cpp 零改动。；**v58 审查 H1 + 正式矩阵前收口（2026-08-09，commit 后附，673 pytest 全绿）：**① run_decision_validation 每完成一个 session 立即同步 self.decision_sessions（函数开始清空、异常保留已完成；测试调用真实方法、子方法替换，禁止 monkeypatch 整方法手写属性，原 test_dv_failure_preserves_partial_sessions 重写）；② calibration report decision_validation 统一 sch.build_decision_validation 标准结构 {sessions,total_valid_rate,partial}（complete 由 partial 派生、成功/异常路径均改，不造第二 schema；errors 结构化 {code,stage,count,detail} v57+ 与旧字符串不兼容）；③ 校准长度缺失改独立 CalibrationLengthMissing 异常（不入 _INFRA_EXCEPTIONS、warmup 不吞、formal/warmup 均不可恢复 → run_safe 70；ServerError 仅可恢复基础设施）；④ _read_log_refresh 流式筛选 key lines（11 个关键 pattern，不整文件 f.read）读失败回退快照；started_at 统一 RFC3339 UTC（sch.now_utc Z 后缀，替换本地偏移 %z）；⑤ --tmp-dir 视为父目录创建本 run 唯一子目录（m0_cal_{run_id}/m0_fanout_{ts}_{pid}）、绝不递归删除用户既有共享目录、cleanup 只删自建子目录（help/文档说明 + 既有目录内容不被删测试）；⑦ 暂不跑 formal（H1 修复后待复审，确认后可立即跑完整 24-unit 4B formal 矩阵）；llama.cpp 零改动。；**v59 审查 H1 + formal 前 Medium/Low（2026-08-09，commit 后附，688 pytest 全绿）：**① calibration runner 日志路径统一 `r.tmp_dir`（M0FanoutRunner 专属子目录）——`rs_observations`/`probe_p10` 的 `_grep_log` 改读 `r.tmp_dir/server_{calib,dv*,p10}.log`（v58 子目录化后旧路径读外层父目录恒空丢观测），补 LogWritingFakeAdapter 真实日志文件集成测试（观测非空 + tag/pid/log_path 正确 + 父目录无 server 日志）；② formal run() dv 阶段基础设施/请求异常 → 合法 preflight 落盘（`run_decision_validation` 捕获 session 级 `_INFRA_EXCEPTIONS` 停止验证阶段不传播；`run()` not-complete 路径构造 `decision_validation=build_decision_validation(self.decision_sessions, partial=True)` 保留已完成 session——原落盘 null 丢证据；AssertionError/KeyError 等内部 bug 仍 run_safe 70；v58 旧 TimeoutError 传播断言改写为捕获语义；第 2 control 中途 openai 连接异常两测试断言 partial dv 落盘）；③ calibration report `date` 统一 `sch.now_utc()`（RFC3339 UTC Z 替换 %z）；④ `_stop_server` 捕获 stop 异常记 `_stop_errors` 不传播（finally 不掩盖 primary；仅无 primary 时 stop 失败按基础设施——dv break / formal FormalIncomplete；4 测试）；⑤ 日志筛选 pattern 收敛单一常量（`LOG_KEY_LINE_KEYS`/`LOG_RS_BUFFER`/`LOG_CAPABILITY`/`LOG_KV_BUFFER`），`_read_log_refresh` 与 `server_logs_summary` 共用防漏同步；⑥ formal tmp 策略明确（专属子目录 -lv5 完整日志保留 results 供排障不视为泄漏、baseline 只归档 key lines）+ 新增 `sch.cleanup_stale_dirs`（只删 `m0_fanout_`/`m0_cal_` 前缀且 mtime 超龄的一级子目录，不删活跃/父目录/非目录文件，main 复用 `--cleanup-tmp-age`）；**下一步可直接运行完整 24-unit 4B formal 矩阵（H1 收口完成）**；llama.cpp 零改动。；**v60 审查 formal 中断归因（2026-08-09，commit 后附，706 pytest 全绿）：**① `_stop_server` 检查 `adapter.stop()` 返回 `(ok,detail)`——ok=False 视为 stop failure（SIGTERM 超时转 kill 等）记录 `_stop_errors`+detail、返回 False；抛异常路径保留；非 tuple 返回（旧 mock None）视为干净；dv/formal 结果可诊断；补真实返回 False 而非抛异常测试（dv session break / formal FormalIncomplete）；② **formal `_capture_baseline`/`_connect` 基础设施异常（OSError/openai/ServerError 含 TimeoutError）必须转 FormalIncomplete**——首 formal group（无 completed group）按首 group preflight/PREFLIGHT_INFRA（FirstGroupStartFailed → 合法 preflight 落盘、保留完整 decision_validation）；第 2+ group 保留已完成 groups + 失败 group ERROR(endpoint_unavailable) + FormalIncomplete 合法落盘（phase=formal/FORMAL_INCOMPLETE、exit 0），**绝不 exit70 丢结果**；内部 AssertionError/KeyError/TypeError 仍 70；补首 group 与第 2+ group OSError/openai e2e + KeyError 仍 70 反例；③ **cleanup_stale_dirs 并发安全**：`--cleanup-tmp-age` 最小值钳制 `<300→300`（`CLEANUP_AGE_MIN=300.0`）+ 文档明确禁止共享父目录并发清理；**每 run marker `ACTIVE.marker`{pid,create_ts,keep}**（runner 建专属 tmp 子目录后写）——cleanup 只删无活跃 marker 且过期目录（keep=true 永不删、pid 存活 `os.kill(pid,0)` 不删、pid 死按 age 删）；keep-tmp 生命周期明确（无 --keep-tmp 参数：pid 退出+age 超龄删除；用户改 marker keep=true 或改名目录避免意外清理）；marker 为控制元数据、`_dir_newest_mtime` 排除；补活跃不删/死 pid 删/keep 保留/marker 不计入测试；④ `_dir_newest_mtime` 含递归子目录 mtime；清理 dv 不可达注释（_finalize 0 group 防御性注释更新）；**删除无调用点冗余 stop try/except 死代码 `_cleanup_all`**（v58/v59 测试改用 `_stop_server`）；**下一步不再改代码、直接运行完整 24-unit 4B formal 矩阵（待用户指令）**；llama.cpp 零改动。；**完整 24-unit 4B formal 矩阵运行（2026-08-09，v63-v65，commit 7d777bf/5acf962/a48a06f 后 run5；**v66 审计降级 INVALID_INTERMEDIATE，见下**）：**12 server groups 全 COMPLETED、24 units × 5 reps=120、(unit_id,rep_index) 唯一、matrix_complete=true、planned/executed=24/24、rejections=0；gates G-M0-1/2/3a/4/5/7 PASS、G-M0-3b/6 NOT_APPLICABLE；**verdict=INVALID_RESULTS_SCHEMA_OR_INFRASTRUCTURE（REP_ERROR：36/120 reps connection_error=30%）**；**v66 审计结论（2026-08-09）：设计 §3.6/§3.8 要求的 transient retry 未接入实际调用（Driver sdk_max_retries=0 且 runner 所有 chat 直调 driver，无外层 wrapper）——run5 的 36/120 connection_error **不能称重试耗尽**，run5 降级 **INVALID_INTERMEDIATE**（中间结果，不作为正式矩阵结论）；**撤销**「已重试 3 次 / 非代码 bug / 稳定基础设施现象 / 有效 off-on 性能结论」等声称——off/on 对比（OK reps latency 528 vs 507ms -4.1%、ttft 287 vs 277ms -3.4%）为噪音级且不具有效性；run1/2/3/5 均 17-30% connection_error 只能说明**无 transient retry 时单线程 server 高并发负载的原始失败率**；shared_cells 恒 0、G-M0-5 PASS（4B hybrid C1 被拒）等**与 retry 无关的结构性结论保持**；**矩阵实测暴露并修复 3 个实现 bug（v63/v64/v65）**：G-M0-4 _parse_rs_buffer_mib 多行求和（201+201=402 超差 → 取首个）、G-M0-5 匹配串（真实 4B 行 'memory implementation does not support...' vs 原 'hybrid'）、rep 缺 latency_ms/ttft_ms（§5.2 定义未落 → build_rep 增字段 + _run_unit 聚合）；run1/2/3 为修复前中间结果（run1 有 G-M0-4/5 FAIL bug、run2/3 有 notes 噪音），**run5 归档（标注 INVALID_INTERMEDIATE）**：`benchmark/baseline/qwen35-4b_gpu_m0_formal_run5_20260809.json` + `..._evidence_20260809.json`（命令/环境/矩阵/gates；evidence 内 `audit_status=INVALID_INTERMEDIATE`）+ **`..._logevidence_20260809.json`（v66：server_logs_key_lines 15 tag 已入 baseline，不再只引用 gitignore results 下文件）**；**限制（v66 保留）**：rep 级 peak_gpu_mb/peak_rss_mb 曾为 0.0 占位（v66 已实现成功 row 取 max；run5 数据仍为旧占位值）；G-M0-6 NOT_APPLICABLE（单次 run 无复现对比）；llama.cpp 零源码改动（仅重建 8570/4a699aaad）。；**v66 transient retry wrapper 实现（2026-08-09，commit 后附）：**单一 `M0FanoutRunner._chat` wrapper 接入全部 5 处 chat 调用（calibration parity `_calibrate_template`、decision validation `_run_decision_session`、formal decision、ThreadPool branch、tool round）；**可重试** = APIConnectionError/APITimeoutError/HTTP status>=500，最多 3 次逻辑调用（首次+重试 2，退避 0.25/0.5）；**不重试** = HTTP 4xx、malformed 成功响应（APIResponseValidationError）、内部 bug（AssertionError/KeyError/TypeError/ValueError）；**每次异常后 poll server**——已退出 → 立即 ServerCrash（不再重试，ThreadPool branch barrier 不因 retry sleep 死锁）；`classify_error` 增强按 openai 异常类型 + status_code 精确分类（APITimeoutError 是 APIConnectionError 子类，先判 timeout；APIResponseValidationError → malformed_response）；SDK max_retries 保持 0（_connect 不变，6-wire 上限由外层 wrapper 单独控制）；mock_server 增 `set_fail(n, status)` 连续 chat 失败注入（只作用于 chat，/apply-template、/tokenize 等不受影响）；rep `metrics.peak_gpu_mb/peak_rss_mb` 改为 decision/branch/tool **成功 row** 的 rss_mb/gpu_mb 取 max（None 过滤；ERROR rep 保留已有成功样本，不再 0.0 占位）；新增 `test_m0_v66_retry.py` 21 例（wrapper 单元 5：首失败后成功/连续 3 次→ERROR/HTTP400 一次/HTTP500 三次/重试期间 server 退出→ServerCrash；**分类单元 8**：APITimeoutError→timeout、APIConnectionError→connection_error、5xx 重试/4xx 不重试、malformed 不重试、内部 bug 不重试、未知内部异常（IndexError/RuntimeError）不重试回归、urllib HTTPError 5xx/4xx；阶段覆盖 5：calibration parity、dv、formal decision、branch barrier 无死锁、tool round；peak 采样 3：成功 row max/取 max/ERROR 保留样本）+ 适配 3 处旧测试（v49 warmup 一次性 500 改连续 3 次、v50 _ToolFailDriver 连续 3 次 500、v59 _FakeDrv fail_at 起连续 3 次）；**v67 复审收口（2026-08-09，commit 后附）：**`_run_decision_session` 显式捕获 ServerCrash（当前请求计入 requests/error_count/error_summary code=server_crash、立即 break 不再执行剩余请求）；`run_decision_validation` 依 requests<target 停止后续 session（崩溃后不再启动新 server；partial session 保留 → partial dv → 合法 PREFLIGHT_INFRA）；新增 `test_m0_v67_review.py` 3 例（dv 首/第二 session 崩溃正反 e2e）；`_transient_class` 文本 fallback 与 classify_error 统一 connection 模式（connection/refused/connect；openai 类型分支优先不变）；mock_server 删无引用 `_ERROR_ONCE`、`_FAIL` 注入显式限定 /v1/chat/completions；并发测试 Lock 保护注入恰好一次 + 断言 retry 发生（_FAIL.remaining==0）；**v68 复审 2 Medium 修复（2026-08-09，commit 后附）：**`_run_decision_session` 返回显式 `(recs, crashed)` 信号，`run_decision_validation` 据 crashed 无条件停止后续 session（不再用 len(recs)<target 推断——第 target 个请求崩溃时 len==target 仍须停止）；覆盖第 1/5/10 请求 ServerCrash 边界（均当前请求计 error 且不启动后续 server）；文本 fallback 收紧为单一 `_text_transient` helper（`_transient_class`/`classify_error` 共用）：timeout 匹配 timed out/timeout，connection 匹配 `\bconnection\b`（refused/reset/error/lost 等）或 failed to connect/unable to connect/cannot connect，**裸 connect 不再匹配**（disconnect/reconnect/connectivity 不误判）；**v69 两轮正式矩阵 run6/run7（2026-08-09，commit 后附）：**均 `verdict=PASS`（120/120 OK、0 ERROR——vs run5 36/120 connection_error，transient retry 为根因修复）；gates G-M0-1/2/3a/4/5/7 PASS、G-M0-3b N/A；**G-M0-6 跨轮判定 PASS**（latency dev 3.29%、ttft dev 2.15%、fallback 全 False）；严格 paired off/on（每 run 60 对）：run7 fanout=f8 同时 latency +6.88%/ttft +10.27% 为 n=10 噪音例外，其余维度 ±≤3%、无系统性 on 收益；分支归属一致（跨轮 120 对 + off-on 配对各 60 对 decision_fallback mismatch 均 0）；**两层结论：A) benchmark/归因门禁 PASS；B) on 机制 4B hybrid 被 capability gate 拒绝（主证据 on g6-g11 capability rejection 日志，共享未建立；24 个 group baseline /metrics/kv 快照 shared_cells 全 0 仅一致性佐证）→ 优化收益 NOT_APPLICABLE/NO_GAIN，不得称共享优化 PASS**；G-M0-6 为归档后跨轮计算（非单 run 机器 gate）；归档 `benchmark/baseline/`（run6/run7 主 JSON+evidence+15-tag key-lines+paired JSON），run5 保持 INVALID_INTERMEDIATE；无代码改动、llama.cpp 零改动；最终结论文档 `docs/M0_FINAL_RESULT.md`。；全量 pytest 758 passed；llama.cpp 零改动。；全量 pytest 752 passed；llama.cpp 零改动。；全量 pytest 见 v66 commit；**未跑完整 GPU 矩阵（wrapper 生效后 run5 结论不成立，重跑待用户指令）**；llama.cpp 零改动。
- **M0 v50 真实失败归因收口（2026-08-09，commit 后附，600 passed）：after_erase 观测缺失走真实可达分支（/metrics/kv fetch 失败 → snapshot None → 显式 EraseFailure('after_erase_missing') → group ERROR + FORMAL_INCOMPLETE，不靠 mock 抛异常，补 _fetch 失败/None 生产路径测试）；校准阶段 ServerCrash/清理异常 → PREFLIGHT_INFRA 合法落盘（run() 宽捕获，保留已收集 preflight_rejections，不得 run_safe exit70，补校准清理崩溃测试）；formal 工具轮 HTTP 异常 server 健康 → 标 rep ERROR（error_type=tool 异常分类）继续严格 erase/after_erase，server 已退出才 ServerCrash（修复原 StopIteration 裸逃逸），补工具轮 500 健康/已退出正反 e2e；error_type 区分 erase_failed/erase_partial/after_erase_failed/after_erase_missing；删 warmup loop 不可达 EraseFailure 分支；warmup best-effort erase 异常（partial）记录可诊断 note 不静默吞（used_cells 先非 0 再证明归零测试）；mock_server 删双 return 死代码；to_json_pointer 完整 RFC6901 转义（~→~0、/→~1，下标占位符方案，9 参数化单测）；失败路径复核：任何请求/erase/观测失败只落设计 preflight/formal 结果或 schema error（内部不变量 bug 除外 run_safe 70）。新增 15 测试（test_m0_v50_failures.py 6 + TestToJsonPointer 9）；**仍未跑正式 4B 矩阵、llama.cpp 零改动**。；**v51 复审阻断修复（2026-08-09，commit 后附，613 passed）：ERROR_CODES 14→16（补录 erase_failed/after_erase_missing 两个 v50 代码已抛未收录码；rep/group validator 正反用例）；完整 run() e2e 触发两种 erase 失败（真实 /metrics/kv 500 → after_erase_missing；clean_all_slots 异常 → erase_failed）——各自 rc=0、phase=formal、FORMAL_INCOMPLETE、matrix_complete=false、ERROR group/rep error_type 正确、最终 envelope validator 通过（禁止只测 _run_unit）；warmup clean_all_slots (0,0) 主动 poll（死 → ServerCrash、健康 → note+继续；真实 /slots 畸形 healthy 正例 + poll dead 反例）；校准 catch-all 不吞内部 bug（只捕 ServerError/ServerCrash/EraseFailure/_INFRA_EXCEPTIONS 网络 OS 异常 → PREFLIGHT_INFRA；AssertionError/KeyError/TypeError → run_safe INTERNAL_RUNNER_ERROR exit70，正反测试）；warmup 工具轮异常与决策异常一致（死 → ServerCrash、健康 → note 不静默；正反 e2e）；preflight envelope builder 支持 notes 参数并保留 runner notes（validator 22 键不变、note 落盘测试）；mock_server 增 slots_malformed 开关；新增 13 测试（test_m0_v51_review.py）；未跑正式 4B 矩阵、llama.cpp 零改动**。；**v52 复审唯一 High 与诊断一致性修复（2026-08-09，commit 后附，619 passed）：校准 Driver.chat 真实 openai SDK 异常显式归 PREFLIGHT_INFRA——`_calibrate_template` 显式捕获 `openai.OpenAIError`（覆盖 APIStatusError/APIConnectionError/APITimeoutError，Driver.chat 原样透传、mock 实测 openai.InternalServerError/APIConnectionError → rc=0 preflight/PREFLIGHT_INFRA/validator 通过），不再依赖 patch run_calibration 抛 HTTPError；内部 AssertionError/KeyError 仍不捕获 → run_safe 70（真实 Driver.chat 路径正反测试）；`_INFRA_EXCEPTIONS` 清理冗余为 `(OSError, openai.OpenAIError)`（HTTPError/URLError/TimeoutError/ConnectionError 均为 OSError 子类；语义注释保留）；warmup 决策请求异常与工具轮一致（server 健康 → 记可诊断 note 不静默、实测第 27 次 chat = 校准 3×2 + 验证 2×10 后首个 warmup 决策；死 server → ServerCrash → group ERROR/FORMAL_INCOMPLETE，正反 e2e）；mock_server slots_malformed 修正为真实 (0,0) 路径（/slots 返回空列表 → list_slots 空 → clean_all_slots (0,0)），删除"非 JSON → erase_failed 生产路径"误导注释（list_slots 对非 JSON/非 200 一律返回 [] 吞错、该路径不可达）；erase_failed 保留为框架防御码（monkeypatch e2e）；校准后 clean_all_slots 异常或 (0,0) + server 健康 → 记可诊断 note、死 server → ServerCrash → 上层 PREFLIGHT_INFRA（补最小测试 3）；TestPreflightNotes 保留 notes 断言改真实 openai 异常路径；新增 6 测试净 613→619；未跑正式 4B 矩阵、llama.cpp 零改动**。
- **M0 v49 复审 3 Medium + 相关 Low 修复（2026-08-09，commit 后附，585 passed）：G-M0-2 基于成功分支观测语义（全矩阵无任何非 ERROR 分支观测 → NOT_APPLICABLE 绝不静默 PASS；ERROR rep 跳过、部分成功观测正常判定）；erase 异常路径（server 健康但 clean_all_slots 部分成功/after_erase 采样异常 → EraseFailure → rep ERROR + group ERROR(error_type=erase_partial/after_erase_failed) + FORMAL_INCOMPLETE 带诊断，不裸异常 exit70；server 已退出 → ServerCrash 归因不变；严格 partial 检查仅 formal rep，warmup erase 由 finally best-effort 承担）；clean_all_slots 返回 (attempt,ok) 检查（partial → 立即 EraseFailure 不等 gate 间接发现）；periodic 迟到样本不覆盖 after_erase（run_aggregate last 优先 tag=after_erase；stop join 超时后 daemon 线程仍最终退出、不跨 run 污染）；warmup 决策异常即使 server 健康也执行 best-effort erase（异常后 poll：崩溃 → group ERROR、健康 → 继续）；校准后清理容错（异常后 poll 同规则）；ERROR_CODES 12→14（+erase_partial/after_erase_failed）；sidecar path 转 RFC6901 JSON Pointer（`sch.to_json_pointer`，修 v30 合同与实现不一致）；mock_server 增 _ERROR_ONCE 一次性 500；**仍未跑正式 4B 矩阵、llama.cpp 零改动**。
- **M0-Q8（q8_0/混合 KV 容量路线）**：**状态：IMPLEMENTED_PROBE（2026-08-21；正式 32K paired matrix 未完成）**。设计合同见 `docs/M0_Q8_HYBRID_KV_CAPACITY_DESIGN.md`，本轮结果见 `docs/M0_Q8_HYBRID_KV_CAPACITY_RESULT_20260821.md`。已实现 additive `/metrics/kv` 分组件观测（attention 与 recurrent R/S bytes、rows、`n_seq_max`、`n_rs_slots`；不可用字段为 `null`），并贯通 `--cache-type-r/--cache-type-s` 与 `--n-rs-slots`。Qwen3.5-4B、ctx4096、parallel=4 实测：F16 attention=128 MiB、Q8 attention=68 MiB（-46.88%）、Q4 attention=36 MiB（-71.88%）；recurrent F32 三档均 201 MiB，attention+recurrent component total 分别为 329/269/237 MiB。Q8/F32 与 F16/F32 的 8K exact-token needle paired 探针 prompt/completion tokens 完全一致，early/middle/late recall 均 3/3；Q8 p50 latency 3248.2 ms vs F16 3272.4 ms（-0.74%），但仅单轮探针，不能升级正式 validated profile。当前 CUDA recurrent graph 的 F16/BF16 在 `ggml_cuda_op_scale` 要求 F32，已显式 `REJECTED_FOR_QWEN35`，无隐式 fallback；`n_rs_slots<n_seq_max` 因现有 seq_id 直映物理 row 已显式拒绝。Q4 仅启动/smoke 通过，保持 `NOT_VALIDATED`。最终 benchmark 回归 `762 passed`；32K formal paired matrix 尚未执行。
- **关键结论：KV 预分配固定 → 生命周期策略不能降显存峰值；lru 价值在"池满防 OOM + 压力下保热点"；q8_0 为上游既有参数组合（非新算法），不宣称主模型算法优化。**

- **KV 分层与热度淘汰（2026-08-22）**：**状态：IMPLEMENTED_PROBE**。支持 `--kv-hotness off|recency|lfu|cost` 与 `--kv-tiering none|ram|ram,disk`；sequence state 可在 GPU、RAM prompt-cache、进程内磁盘冷层间迁移，pressure/idle victim 使用 session 热度选择，active/shared source 受保护。默认 off/none 保持旧行为；启动预分配 capacity 不下降；hybrid/recurrent over-long state 安全拒绝 restore 并全量 prefill。CUDA 定向回归 12 passed；Qwen3.5-4B A1/A2/B1/C1 全部 PASS：A1 recency p50 205.61 vs control 200.92 ms（+2.34%，hash 同）；B1 RAM recall 234.11 vs control 261.37 ms（-10.43%，used cells 305→0，hash 同）；C1 RAM+disk recall 210.56 vs control 225.23 ms（-6.51%，L2 spill 1、57.9 MiB、hash 同）；A2 正确淘汰 oldest idle 且 active 未清。A1 仅 3-request probe，不能宣称稳定性能收益；token 级有损淘汰与跨重启 L2 未实现。详见 `docs/KV_TIERED_STORAGE_AND_HOTNESS_EVICTION_RESULT_20260821.md`，证据归档 `benchmark/baseline/qwen35-4b_gpu_kv_hotness_tiering_final_20260821.json`。
